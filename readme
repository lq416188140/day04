数组的概述。
数组是指一组数据的集合，数组中的每个数据称作 元素
在数组中可以存放任意类型的元素，但同一个数组里存放的类型必须一致。
定义数组的公式：
    数据类型[] 变量名=new 数据类型[存储元素的个数，或数组的长度]，

    数据类型：表示数组中存储元素的数据类型
    []表示数组的意思。
    new 表示创建容器的关键字
    元素个数：指的是数组中可以多少个数据（恒定，定长）

    数组是一个容器。存储到数组中的元素，都有自己的自动编号。
    自动标号最小值是0，最大值是数组长度-1，
    自动编号专业名词，索引（index），下标，角标。
    访问数组中存储的元素，必须依赖索引。 公式：数组名[索引]
    Java提供一个属性来操作索引；
    数组的一个熟悉，就是数组的长度，属性的名字 length
    使用属性     数组名.length 数据类型为 int
    数组的最小索引是0.最大索引是 数组.length-1.
    数组的另外俩种定义方式：
     * 1、数据类型 [] 变量名=new 数据类型[]{元素1,元素2.....};//
     *      注意事项；new后面的[]不允许写任何内容，写了就编译失败。
     * 2、数据类型 [] 变量名={元素1，元素2......}；
          数据类型[] 变量名=new 数据类型[存储元素的个数，或数组的长度]，
          数组的俩种异常：
          1、java.lang.NullPointerException（空指针异常）
          2、